# Build Images and Deploy to ECR 

### 1. aws-cli should be available and configured before ahead

use the following command:-

```
terraform apply -auto-approve && terraform destroy -auto-approve
```